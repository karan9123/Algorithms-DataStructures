#include <iostream>
#include <vector>

int main(){
    size_t size;
    std::cout<< "Enter the number of elements:" << std::endl;
    std::cin >> size;
    std::vector<std::vector<int>> hospital_choice_vec;
    std::vector<std::vector<int>> resident_choice_vec; 

    std::cout<<"Enter";
    for(size_t i = 0; i< size; i++){
        
    }
    return 0;
}